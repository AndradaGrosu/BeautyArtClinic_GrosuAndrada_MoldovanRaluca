@page "{id:int?}"
@model BeautyArtClinic_GrosuAndrada_MoldovanRaluca.Pages.Medici.IndexModel

@{
    ViewData["Title"] = "Index";
}


<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Medic[0].NumeMedic)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Medic[0].Departament)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Medic) 
        {
           


        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumeMedic)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Departament.NumeDepartament)
            </td>
            <td>
              
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
